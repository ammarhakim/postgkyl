name: pgkyl
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.10
  # Runtime and test dependency versions have one authoritative home in
  # pyproject.toml. Install the project after activating this environment.
  - pip
  - setuptools>=61.0
